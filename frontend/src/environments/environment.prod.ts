// Producción (GitHub Pages): SPA estática, lee JSON precomputados y calcula las
// partes interactivas en el navegador. No requiere backend.
export const environment = {
  staticMode: true,
  apiBase: '',
};
